\section{Execution Layer Position in the AI-Native Stack}
\label{sec:stack}

\subsection{Five-Layer Architecture}

The execution layer sits within a broader AI-Native implementation stack. In the final arXiv bundle, the stack overview will be supplied as a dedicated vector figure. The current conversion pass embeds a LaTeX-native schematic to preserve semantics and compile without external graphics.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  layer/.style={draw=black!35, rounded corners=3pt, minimum width=10.5cm, minimum height=1.1cm, align=center, font=\bfseries},
  sublayer/.style={draw=black!30, rounded corners=2pt, minimum width=7.4cm, minimum height=0.8cm, align=center, font=\small},
  flow/.style={-{Latex[length=2.2mm]}, thick, draw=black!55}
]
  \node[layer, fill=blue!10] (ui) at (0,0) {User Interface};
  \node[layer, fill=cyan!10, below=0.9cm of ui] (apps) {AI-Native Applications\\\normalfont(Agents, Workflows, Applications)};
  \node[layer, fill=teal!10, below=0.9cm of apps] (cog) {AI Cognition Layer\\\normalfont(LLMs, Reasoning, Planning)};
  \node[layer, fill=orange!10, below=1.1cm of cog, minimum height=2.7cm] (action) {AI Action Layer};
  \node[sublayer, fill=orange!20] (orch) at ([yshift=0.45cm]action.center) {Policy Orchestration Layer};
  \node[sublayer, fill=yellow!18] (runtime) at ([yshift=-0.55cm]action.center) {Execution Runtime};
  \node[layer, fill=purple!10, below=1.1cm of action] (eco) {Capability Ecosystem\\\normalfont(Models, APIs, Tools, Devices)};
  \node[layer, fill=gray!12, below=0.9cm of eco] (infra) {Infrastructure\\\normalfont(Cloud, Edge, Networks)};

  \draw[flow] (ui) -- (apps);
  \draw[flow] (apps) -- (cog);
  \draw[flow] (cog) -- (action);
  \draw[flow] (action) -- (eco);
  \draw[flow] (eco) -- (infra);
  \draw[flow] ([xshift=0.2cm]orch.south) -- ([xshift=0.2cm]runtime.north);
\end{tikzpicture}
\caption{Five-layer AI-Native implementation stack with the action layer decomposed into orchestration and execution.}
\label{fig:five-layer-stack}
\end{figure}

\subsection{Action Layer Decomposition}

A critical insight is that the action layer comprises two subcomponents:

\paragraph{Orchestration component.}
The orchestration component is responsible for capability routing, provider selection, policy evaluation, and environment selection.

\paragraph{Execution component.}
The execution component is responsible for API invocation, tool execution, error handling, and result normalization.

This decomposition maintains clear separation of concerns. The orchestration layer makes decisions, such as which provider or environment to use, while the execution layer performs actions, such as invoking APIs and returning normalized results.

When mapped to the logical model in \cref{eq:logical-model}, the orchestration component corresponds to $P$, and the execution runtime corresponds to $E$.

\begin{table}[t]
\caption{Mapping between the logical model and the implementation stack.}
\label{tab:logical-vs-implementation}
\centering
\small
\begin{tabularx}{\linewidth}{@{} l l X @{}}
\toprule
Logical element & Implementation mapping & Responsibility \\
\midrule
$A$ & AI Cognition Layer & Reasoning, planning, memory, and task decomposition. \\
$P$ & Policy Orchestration Layer & Routing, provider selection, policy evaluation, and workflow control. \\
$E$ & Execution Runtime & Deterministic capability invocation, protocol translation, result normalization, and execution telemetry forwarding. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Defining Statement}

\begin{quote}
\emph{The execution layer is the action interface between AI cognition and the real-world capability ecosystem.}
\end{quote}

This definition captures the essential role of the layer: mediating between what agents decide to do and how those decisions are realized through capability execution.
